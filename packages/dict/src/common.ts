export enum Lang {
  EN = 'en',
  DE = 'de'
}

export enum Format {
  PLAIN = 'plain',
  UNICODE = 'unicode',
  LATEX = 'latex'
}

export enum SIType {
  BASE = 'base',
  DERIVED = 'derived',
  ACCEPTED = 'accepted'
}
